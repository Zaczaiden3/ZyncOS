import React from 'react';

const CoreLoader: React.FC = () => (
  <div className="flex items-center justify-center h-full w-full text-cyan-500 font-mono text-xs animate-pulse">
    [INITIALIZING_MODULE...]
  </div>
);

export default CoreLoader;
